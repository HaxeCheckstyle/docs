---
title: Nested For Depth
toc: false
---

Restricts nested `for` blocks to a specified depth (default = 1).

### Configuration

```json
{
    "type": "NestedForDepth",
    "props": {
        "severity": "ERROR",
        "max": 1
    }
}
```