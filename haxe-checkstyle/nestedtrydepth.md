---
title: Nested Try Depth
toc: false
---

Restricts nested `try` blocks to a specified depth (default = 1).

### Configuration

```json
{
    "type": "NestedTryDepth",
    "props": {
        "severity": "ERROR",
        "max": 1
    }
}
```